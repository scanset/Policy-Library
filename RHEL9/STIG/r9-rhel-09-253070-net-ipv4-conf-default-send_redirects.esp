# STIG RHEL-09-253070: sysctl net.ipv4.conf.default.send_redirects
# STIG ID: V-257969 (MEDIUM)

META
    esp_id `r9-rhel-09-253070-net-ipv4-conf-default-send_redirects-001`
    version `1.0.0`
    dsl_schema_version `1.0.0`
    platform `rocky9`
    criticality `high`

    title `STIG RHEL-09-253070: RHEL 9 must not allow interfaces to perform Internet Control Message Protocol (I`
    description `RHEL 9 must not allow interfaces to perform Internet Control Message Protocol (ICMP) redirects by default.`
    author `example-security`
    tags `rhel9,stig,disa-stig,kernel,sysctl`

    control_mapping `DISA-STIG:RHEL-09-253070,NIST-800-53:CM-6`
    control_objective `CM-6`

    assessment_method `TEST`
    implementation_status `implemented`
    responsible_role `system-admin`

    control_origination `sp-system`
    inherited_from `Rocky9:localhost:os-hardening`
    customer_responsibility `configuration`

    remediation_days `30`
    recommendation `Review and remediate per DISA STIG guidance for RHEL-09-253070.`

    component_id `comp-r9-os`

    impl_CM_6 `This control is validated by checking the required system configuration against the DISA STIG baseline for RHEL 9.`

META_END

DEF
    OBJECT param_check
        parameter `net.ipv4.conf.default.send_redirects`
    OBJECT_END

    STATE expected_value
        found boolean = true
        value string = `0`
    STATE_END

    CRI AND
        CTN sysctl_parameter
            TEST all all AND
            STATE_REF expected_value
            OBJECT_REF param_check
        CTN_END
    CRI_END
DEF_END
