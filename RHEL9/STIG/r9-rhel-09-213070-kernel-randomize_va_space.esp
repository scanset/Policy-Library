# STIG RHEL-09-213070: sysctl kernel.randomize_va_space
# STIG ID: V-257809 (MEDIUM)

META
    esp_id `r9-rhel-09-213070-kernel-randomize_va_space-001`
    version `1.0.0`
    dsl_schema_version `1.0.0`
    platform `rocky9`
    criticality `high`

    title `STIG RHEL-09-213070: RHEL 9 must implement address space layout randomization (ASLR) to protect its m`
    description `RHEL 9 must implement address space layout randomization (ASLR) to protect its memory from unauthorized code execution.`
    author `example-security`
    tags `rhel9,stig,disa-stig,kernel,sysctl`

    control_mapping `DISA-STIG:RHEL-09-213070,NIST-800-53:SI-16`
    control_objective `SI-16`

    assessment_method `TEST`
    implementation_status `implemented`
    responsible_role `system-admin`

    control_origination `sp-system`
    inherited_from `Rocky9:localhost:os-hardening`
    customer_responsibility `configuration`

    remediation_days `30`
    recommendation `Review and remediate per DISA STIG guidance for RHEL-09-213070.`

    component_id `comp-r9-os`

    impl_SI_16 `This control is validated by checking the required system configuration against the DISA STIG baseline for RHEL 9.`

META_END

DEF
    OBJECT param_check
        parameter `kernel.randomize_va_space`
    OBJECT_END

    STATE expected_value
        found boolean = true
        value string = `2`
    STATE_END

    CRI AND
        CTN sysctl_parameter
            TEST all all AND
            STATE_REF expected_value
            OBJECT_REF param_check
        CTN_END
    CRI_END
DEF_END
