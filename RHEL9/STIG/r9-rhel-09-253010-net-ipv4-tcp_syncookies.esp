# STIG RHEL-09-253010: sysctl net.ipv4.tcp_syncookies
# STIG ID: V-257957 (MEDIUM)

META
    esp_id `r9-rhel-09-253010-net-ipv4-tcp_syncookies-001`
    version `1.0.0`
    dsl_schema_version `1.0.0`
    platform `rocky9`
    criticality `high`

    title `STIG RHEL-09-253010: RHEL 9 must be configured to use TCP syncookies.`
    description `RHEL 9 must be configured to use TCP syncookies.`
    author `example-security`
    tags `rhel9,stig,disa-stig,kernel,sysctl`

    control_mapping `DISA-STIG:RHEL-09-253010,NIST-800-53:SC-5,NIST-800-53:SC-5(2)`
    control_objective `SC-5`

    assessment_method `TEST`
    implementation_status `implemented`
    responsible_role `system-admin`

    control_origination `sp-system`
    inherited_from `Rocky9:localhost:os-hardening`
    customer_responsibility `configuration`

    remediation_days `30`
    recommendation `Review and remediate per DISA STIG guidance for RHEL-09-253010.`

    component_id `comp-r9-os`

    impl_SC_5 `This control is validated by checking the required system configuration against the DISA STIG baseline for RHEL 9.`

META_END

DEF
    OBJECT param_check
        parameter `net.ipv4.tcp_syncookies`
    OBJECT_END

    STATE expected_value
        found boolean = true
        value string = `1`
    STATE_END

    CRI AND
        CTN sysctl_parameter
            TEST all all AND
            STATE_REF expected_value
            OBJECT_REF param_check
        CTN_END
    CRI_END
DEF_END
