# STIG RHEL-09-213075: sysctl kernel.unprivileged_bpf_disabled
# STIG ID: V-257810 (MEDIUM)

META
    esp_id `r9-rhel-09-213075-kernel-unprivileged_bpf_disabled-001`
    version `1.0.0`
    dsl_schema_version `1.0.0`
    platform `rocky9`
    criticality `high`

    title `STIG RHEL-09-213075: RHEL 9 must disable access to network bpf system call from nonprivileged process`
    description `RHEL 9 must disable access to network bpf system call from nonprivileged processes.`
    author `example-security`
    tags `rhel9,stig,disa-stig,kernel,sysctl`

    control_mapping `DISA-STIG:RHEL-09-213075,NIST-800-53:SC-2`
    control_objective `SC-2`

    assessment_method `TEST`
    implementation_status `implemented`
    responsible_role `system-admin`

    control_origination `sp-system`
    inherited_from `Rocky9:localhost:os-hardening`
    customer_responsibility `configuration`

    remediation_days `30`
    recommendation `Review and remediate per DISA STIG guidance for RHEL-09-213075.`

    component_id `comp-r9-os`

    impl_SC_2 `This control is validated by checking the required system configuration against the DISA STIG baseline for RHEL 9.`

META_END

DEF
    OBJECT param_check
        parameter `kernel.unprivileged_bpf_disabled`
    OBJECT_END

    STATE expected_value
        found boolean = true
        value string = `1`
    STATE_END

    CRI AND
        CTN sysctl_parameter
            TEST all all AND
            STATE_REF expected_value
            OBJECT_REF param_check
        CTN_END
    CRI_END
DEF_END
