# STIG CNTR-K8-001160: Secrets in Kubernetes must not be stored as environment vari
# STIG ID: V-242415 (HIGH)

META
    esp_id `k8s-cntr-k8-001160-secrets-as-env-001`
    version `1.0.0`
    dsl_schema_version `1.0.0`
    platform `kubernetes`
    criticality `critical`

    title `STIG CNTR-K8-001160: Secrets in Kubernetes must not be stored as environment variables.`
    description `Secrets in Kubernetes must not be stored as environment variables.`
    author `example-security`
    tags `kubernetes,stig,disa-stig`

    control_mapping `DISA-STIG:CNTR-K8-001160,NIST-800-53:IA-5(1)(D)`
    control_objective `IA-5(1)(D)`

    assessment_method `TEST`
    implementation_status `implemented`
    responsible_role `system-admin`

    control_origination `sp-system`
    inherited_from `Kubernetes:cluster:k8s-hardening`
    customer_responsibility `configuration`

    remediation_days `1`
    recommendation `Review and remediate per DISA STIG guidance for CNTR-K8-001160.`

    component_id `comp-k8s-cluster`

    impl_IA_5_1_D `This control is validated by checking the required Kubernetes configuration against the DISA STIG baseline.`

META_END

DEF
    OBJECT resource_check
        kind `Pod`
    OBJECT_END

    STATE resource_state
         found boolean = true
    STATE_END

    CRI AND
        CTN k8s_resource
            TEST all all AND
            STATE_REF resource_state
            OBJECT_REF resource_check
        CTN_END
    CRI_END
DEF_END
