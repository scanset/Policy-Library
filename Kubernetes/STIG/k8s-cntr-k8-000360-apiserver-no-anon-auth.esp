# STIG CNTR-K8-000360: The Kubernetes API server must have anonymous authentication
# STIG ID: V-242390 (HIGH)

META
    esp_id `k8s-cntr-k8-000360-apiserver-no-anon-auth-001`
    version `1.0.0`
    dsl_schema_version `1.0.0`
    platform `kubernetes`
    criticality `critical`

    title `STIG CNTR-K8-000360: The Kubernetes API server must have anonymous authentication disabled.`
    description `The Kubernetes API server must have anonymous authentication disabled.`
    author `example-security`
    tags `kubernetes,stig,disa-stig,apiserver`

    control_mapping `DISA-STIG:CNTR-K8-000360,NIST-800-53:AC-3`
    control_objective `AC-3`

    assessment_method `TEST`
    implementation_status `implemented`
    responsible_role `system-admin`

    control_origination `sp-system`
    inherited_from `Kubernetes:cluster:k8s-hardening`
    customer_responsibility `configuration`

    remediation_days `1`
    recommendation `Review and remediate per DISA STIG guidance for CNTR-K8-000360.`

    component_id `comp-k8s-cluster`

    impl_AC_3 `This control is validated by checking the required Kubernetes configuration against the DISA STIG baseline.`

META_END

DEF
    OBJECT apiserver_pod
        kind `Pod`
        namespace `kube-system`
        label_selector `component=kube-apiserver`
    OBJECT_END

    STATE flag_check
        found boolean = true
        record
            field spec.containers.0.command string contains `--anonymous-auth=false` at_least_one
        record_end
    STATE_END

    CRI AND
        CTN k8s_resource
            TEST all all AND
            STATE_REF flag_check
            OBJECT_REF apiserver_pod
        CTN_END
    CRI_END
DEF_END
